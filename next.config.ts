import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Files in public/ are automatically served at the root
  // JSON files in public/demo-data/ are accessible at /demo-data/*.json
};

export default nextConfig;
